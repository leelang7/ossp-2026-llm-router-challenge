# SPDX-FileCopyrightText: Copyright 2026 routerx contributors
# SPDX-License-Identifier: Apache-2.0

"""3분 제한에 맞춘 나레이션으로 시연영상을 만든다.

슬라이드와 합성 절차는 make_video.py를 그대로 쓰고 해설만 짧게 바꾼다.
"""
from __future__ import annotations

import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parent))

import make_video as base

SHORT = {
    "s01": "LLM 서비스에서 추론 비용은 가장 큰 지출입니다. 그런데 트래픽의 상당수는 "
           "값싼 모델로도 충분한 질의입니다. 이 라우터는 프롬프트만 보고 세 모델 중 "
           "하나를 골라 정해진 예산 안에서 품질을 최대로 끌어올립니다. 모델을 "
           "호출하거나 답을 비교할 수 없고, 예산을 넘기면 그 등급은 0점입니다.",
    "s02": "실제로 돌려보겠습니다. 공개 검증 자료 880문항을 프리미엄 등급으로 "
           "라우팅합니다.",
    "s03": "공식 채점기 결과입니다. 세 등급 모두 예산 안에 있고 최종 점수는 "
           "영 점 칠일육칠입니다.",
    "s04": "프롬프트에서 직접 계산 특징 36개와 단어, 문자 티에프 아이디에프를 뽑습니다. "
           "여기서 품질과 함께 토큰 사용량까지 예측합니다. 라우팅 시점에 토큰 수가 "
           "주어지지 않기 때문입니다. 그다음 이득 대비 비용이 큰 순서로 승격합니다.",
    "s05": "첫째, 순서 불변성입니다. 예산은 배치 전체에 걸린 제약이라 문항을 독립 "
           "처리할 수 없는데, 입력 순서에 의존하면 규칙 위반입니다. 그래서 동률은 "
           "프롬프트 내용 해시로만 정렬합니다. 순서를 섞고 아이디를 바꿔도 결과가 "
           "같습니다.",
    "s06": "자체 점검 도구입니다. 순서 불변성, 결정성, 엣지 케이스, 실행 시간, 예산 "
           "여유를 한 번에 확인합니다. 열 항목 모두 통과이고 실행 시간은 한도의 "
           "5퍼센트입니다.",
    "s07": "둘째, 예산 안전입니다. 추론 모델은 출력 토큰 중앙값이 천오백인데 최대는 "
           "십삼만입니다. 한 문항이 전체 비용의 26퍼센트를 먹을 수 있습니다. 예측으로 "
           "위험 문항을 거르려 했지만 실패했습니다. 예측은 작은데 실제가 큰 것들이라 "
           "필터를 통과하기 때문입니다. 그래서 선택 건수 자체를 묶었습니다.",
    "s08": "2640문항을 5겹과 8겹으로 나눠 양쪽에서 예산 초과가 없는 조합만 골랐습니다. "
           "이 과정에서 두 번 되돌렸습니다. 부스팅 트리는 단일 측정 최고점이었지만 "
           "교차검증에서 예산을 못 지켰고, 임베딩은 오히려 예측이 나빠졌습니다. "
           "점수 영 점 영일보다 등급 0점이 훨씬 큽니다.",
    "s09": "학습은 사이킷런, 실행은 넘파이 하나입니다. 두 경로의 예측 차이는 "
           "부동소수점 한계까지 일치합니다. 아파치 2.0으로 공개했습니다. 감사합니다.",
}

if __name__ == "__main__":
    for scene in base.SCENES:
        scene["say"] = SHORT[scene["name"]]
    sys.exit(base.main())
