<!--
SPDX-FileCopyrightText: Copyright 2026 routerx contributors
SPDX-License-Identifier: Apache-2.0
-->

# 시연영상 대본 (3분 이내)

> 유튜브 업로드 후 URL을 결과보고서에 기재한다. 별도 파일 제출은 받지 않는다.
> 화면 녹화 + 음성 해설. 코드를 한 줄씩 읽지 말고 **동작과 근거**를 보여준다.

## 구성 (총 2분 50초 목표)

### 0:00–0:25 — 문제

화면: 과제 페이지 또는 슬라이드 한 장

> "LLM 서비스에서 추론 비용은 가장 큰 지출입니다. 그런데 실제 트래픽의 상당수는
> 값싼 모델로도 충분한 단순 질의입니다. 이 라우터는 프롬프트만 보고 세 모델 중
> 하나를 골라, 정해진 예산 안에서 답변 품질을 최대로 끌어올립니다.
> 모델을 호출하지도, 답을 비교하지도 않습니다. 한 번에 정해야 합니다."

핵심 제약 자막: `프롬프트만 입력 · 모델 호출 불가 · 예산 초과 시 해당 등급 0점`

### 0:25–1:10 — 동작 시연

화면: 터미널. 실제로 실행한다.

```console
PYTHONPATH=src python3 -m routerx.cli \
  --input data/materialized/dev/inputs.json --tier premium \
  --output build/premium.json
```

> "공개 Dev 880문항을 premium 등급으로 라우팅합니다. 4초가 걸렸고, 제한은
> 등급당 90초입니다."

이어서 self-check:

```console
PYTHONPATH=src python3 -m ossp_router.cli self-check ...
```

> "공식 채점기로 확인하면 세 등급 모두 예산 안에 있고 최종 점수가 나옵니다."

화면에 점수와 예산 사용률이 보이도록 확대.

### 1:10–2:05 — 어떻게 정하는가 (핵심)

화면: 슬라이드 또는 도식 한 장

> "프롬프트에서 세 가지를 뽑습니다. 길이·한글 비율·수식과 코드 표지 같은 직접
> 계산 특징 36개, 단어 TF-IDF, 문자 TF-IDF입니다. 여기서 모델별 품질과 함께
> **토큰 사용량까지 예측**합니다. 라우팅 시점에는 토큰 수가 주어지지 않기 때문에
> 비용도 예측해야 합니다."

> "그다음 이득 대비 비용이 큰 순서로 승격합니다. 여기서 두 가지를 신경 썼습니다."

**(1) 순서 불변성**

> "예산은 배치 전체에 걸린 제약이라 문항을 독립적으로 처리할 수 없습니다. 그런데
> 입력 순서에 의존하면 규칙 위반입니다. 그래서 동률은 프롬프트 내용의 해시로만
> 정렬합니다. 운영자가 순서를 섞고 문항 ID를 바꿔 재실행해도 결과가 같습니다."

터미널로 감사 도구 실행:

```console
PYTHONPATH=src python3 -m routerx.audit --input ... --outcomes ...
```

> "직접 만든 자체 점검 도구입니다. ID·순서 불변성, 결정성, 엣지 케이스, 실행 시간,
> 예산 여유를 한 번에 확인합니다. 지금 열 항목 모두 통과입니다."

**(2) 추론 모델 사용량 제한**

> "가장 비싼 추론 모델은 출력 토큰이 극단적으로 튑니다. 중앙값은 1,570토큰인데
> 최대는 13만 토큰입니다. 한 문항이 전체 경량 비용의 26%를 먹을 수 있어서,
> 몇 건만 빗나가도 등급 전체가 0점이 됩니다."

> "예측 비용으로 위험한 문항을 걸러내려 했지만 실패했습니다. 사고를 내는 문항은
> '예측은 작은데 실제가 큰' 것들이라 예측 기반 필터를 그대로 통과합니다.
> 그래서 **선택 건수 자체를 묶었습니다.** 예측이 틀려도 노출이 제한됩니다."

### 2:05–2:40 — 검증과 판단

화면: 표 한 장

> "설정은 감으로 정하지 않았습니다. 2,640문항을 5-fold와 8-fold로 나눠, 양쪽에서
> 예산 초과가 한 건도 없는 조합만 후보로 삼았습니다."

> "이 과정에서 두 번 되돌렸습니다. 부스팅 트리는 토큰 예측 상관을 0.37에서
> 0.65로 올렸고 단일 측정에서도 가장 높았지만, 교차검증에서는 어떤 마진으로도
> 예산을 지키지 못했습니다. 임베딩 모델은 오히려 예측이 나빠졌습니다.
> 이 과제의 난이도 신호가 의미 유사도가 아니라 표면 구조에 있기 때문입니다."

> "점수 0.01을 얻으려다 등급 0점을 맞으면 최종 점수의 30%를 잃습니다.
> 측정 결과를 따랐습니다."

### 2:40–2:50 — 마무리

> "학습은 scikit-learn으로, 실행은 NumPy 하나로 합니다. TF-IDF 동작을 표준
> 라이브러리로 재현해 두 경로의 예측 차이가 부동소수점 한계까지 일치합니다.
> 이미지는 73MB, 코드는 Apache 2.0으로 공개했습니다."

## 촬영 준비

- [ ] 터미널 글꼴을 키우고 배경을 밝게(가독성)
- [ ] 실행 전 `build/` 정리해서 결과가 새로 생기는 것이 보이게
- [ ] self-check와 audit 출력이 화면에 다 들어오도록 창 크기 조정
- [ ] 슬라이드 3장: (1) 문제와 제약 (2) 구조 도식 (3) 검증 결과 표
- [ ] 3분을 넘기지 않도록 리허설 1회

## 반드시 담아야 할 것

심사 항목과 직결된다.

1. **실제로 동작하는 화면** — 라우팅 실행과 공식 채점 결과
2. **순서 불변성** — 규칙 준수를 스스로 검증한다는 점
3. **예산 안전 설계** — 왜 건수 제한인지
4. **되돌린 결정** — 측정에 근거해 판단했다는 점(차별성)
5. **실행 의존성이 NumPy 하나** — 재현성과 이식성
